{
  "private": true,
  "scripts": {
    "dev": "next",
    "build": "node buildGhPages && next build && next export && posthtml out/*.html -u posthtml-plugin-relative-urls"
  },
  "dependencies": {
    "next": "^10.1.3",
    "next-offline": "^5.0.5",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "jsonfile": "^6.1.0",
    "posthtml": "^0.15.2",
    "posthtml-cli": "^0.9.1",
    "posthtml-plugin-relative-urls": "^1.0.0"
  }
}
